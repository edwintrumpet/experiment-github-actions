name: Deploy staging

on:
  push:
    branches:
      - staging

jobs:
  deploy-to-gh-pages:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          ref: staging
      - name: Install
        run: npm i
      - name: Build
        run: npm run build
      - name: Deploy
        run: npm run deploy
